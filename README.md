# Task Manager

A simple task manager application.

### Stack

- Frontend: React, TypeScript, Vite
- Backend: Node.js with TypeScript, Express, Prisma
- Database: PostgreSQL

### Local development

In two separate terminals, `cd` into the `frontend` and `backend` directories respectively, and follow the steps in their respective README files.
