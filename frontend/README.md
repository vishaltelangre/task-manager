# Task Manager using React + TypeScript

### Local setup

```bash
cp .env.example .env
npm install
```

Set the appropriate values for all environment variables in `.env` file.

To start the server (which will also show TypeScript errors as well)

```bash
npm run dev
```

To manually compile and watch TypeScript errors, run

```bash
tsc --noEmit --watch
```
